<%= render partial: "mail_preview_modal", locals: {campaign: @campaign} %>

<% if @campaign.sent? %>
  <%= render partial: 'report', locals: {campaign: @campaign} %>
<% else %>
  <%= render partial: 'preview', locals: {campaign: @campaign} %>
<%end%>

